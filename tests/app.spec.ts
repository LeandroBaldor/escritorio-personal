import{test,expect}from'@playwright/test';
test('persiste notas, movimiento, diario y gastos',async({page})=>{await page.goto('/escritorio-personal/');await page.getByPlaceholder('¿Qué necesitás recordar?').fill('Pagar luz');await page.getByRole('button',{name:'Agregar nota'}).click();await page.getByLabel('Mover a').selectOption('doing');await page.getByText('Historial').click();await expect(page.getByText('En progreso:',{exact:false})).toBeVisible();await page.reload();await expect(page.getByLabel('Texto de nota')).toHaveValue('Pagar luz');await page.getByRole('link',{name:'Mi diario',exact:true}).click();page.once('dialog',d=>d.accept('Semana'));await page.getByRole('button',{name:'Nueva carpeta'}).click();await page.getByLabel('Página del diario').fill('Algo importante');await page.getByRole('link',{name:'Gastos',exact:true}).click();await page.getByPlaceholder('Ej. Electricidad').fill('Electricidad');await page.getByPlaceholder('0,00').fill('12,34');await page.getByRole('button',{name:'Agregar'}).click();await expect(page.locator('.total strong')).toContainText('12,34');await page.reload();await expect(page.getByLabel('Concepto')).toHaveValue('Electricidad')});
test('rechaza backup inválido y restaura uno válido con confirmación',async({page})=>{await page.goto('/escritorio-personal/');page.once('dialog',async d=>{expect(d.message()).toContain('versión compatible');await d.accept()});await page.locator('input[type=file]').setInputFiles({name:'bad.json',mimeType:'application/json',buffer:Buffer.from('{}')});const backup={version:1,notes:[{id:'n',text:'Restaurada',color:'#ffe783',status:'todo',history:[{status:'todo',at:'2026-01-01T00:00:00.000Z'}]}],folders:[],expenses:[]};await page.locator('input[type=file]').setInputFiles({name:'ok.json',mimeType:'application/json',buffer:Buffer.from(JSON.stringify(backup))});await expect(page.getByRole('dialog',{name:'¿Restaurar esta copia?'})).toBeVisible();await page.getByRole('button',{name:'Confirmar restauración'}).click();await expect(page.getByLabel('Texto de nota')).toHaveValue('Restaurada')});
