import{Link,NavLink,Outlet}from'react-router-dom';import{useData}from'./DataContext';import{parseBackup,serialize}from'../storage/store';import{useEffect,useRef,useState}from'react';
export function Layout(){const{data,setData,warning}=useData();const input=useRef<HTMLInputElement>(null);const importButton=useRef<HTMLButtonElement>(null);const confirmButton=useRef<HTMLButtonElement>(null);const[pending,setPending]=useState<ReturnType<typeof parseBackup>|null>(null);
 const close=()=>{setPending(null);requestAnimationFrame(()=>importButton.current?.focus())};useEffect(()=>{if(!pending)return;confirmButton.current?.focus();const key=(e:KeyboardEvent)=>{if(e.key==='Escape')close()};addEventListener('keydown',key);return()=>removeEventListener('keydown',key)},[pending]);
 const download=()=>{const url=URL.createObjectURL(new Blob([serialize(data)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download=`escritorio-personal-${new Date().toISOString().slice(0,10)}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(url),0)};
 const pick=async(e:React.ChangeEvent<HTMLInputElement>)=>{const file=e.target.files?.[0];if(!file)return;try{setPending(parseBackup(await file.text()))}catch(err){alert(err instanceof Error?err.message:'Copia inválida')}e.target.value=''};
 return <><header><Link className="brand" to="/">Escritorio Personal</Link><nav aria-label="Principal"><NavLink to="/">Notas</NavLink></nav><div className="backup"><button onClick={download}>Exportar</button><button ref={importButton} onClick={()=>input.current?.click()}>Importar</button><input ref={input} hidden type="file" accept="application/json" onChange={pick}/></div></header>{warning&&<p role="alert" className="warning">{warning}</p>}<main><Outlet/></main>{pending&&<div className="modal" role="dialog" aria-modal="true" aria-labelledby="restore-title"><div><h2 id="restore-title">¿Restaurar esta copia?</h2><p>Contiene {pending.notes.length} notas, {pending.folders.length} carpetas y {pending.expenses.length} gastos. Reemplazará los datos actuales.</p><button ref={confirmButton} onClick={()=>{setData(pending);close()}}>Confirmar restauración</button><button onClick={close}>Cancelar</button></div></div>}</>}
