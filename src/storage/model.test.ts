import{describe,it,expect,beforeEach}from'vitest';import{parseCents,total,EMPTY}from'./model';import{parseBackup,serialize,load,save}from'./store';
describe('datos exactos y backup',()=>{beforeEach(()=>localStorage.clear());it('suma en centavos',()=>expect(total([{id:'1',concept:'a',cents:10},{id:'2',concept:'b',cents:20}])).toBe(30));it('valida montos y overflow',()=>{expect(parseCents('12,34')).toBe(1234);expect(parseCents('-1')).toBeNull();expect(parseCents('999999999999999999')).toBeNull();expect(()=>total([{id:'1',concept:'a',cents:Number.MAX_SAFE_INTEGER},{id:'2',concept:'b',cents:1}])).toThrow()});it('hace round trip',()=>expect(parseBackup(serialize(EMPTY))).toEqual(EMPTY));it('persiste',()=>{save(EMPTY);expect(load().data).toEqual(EMPTY)});it.each([{...EMPTY,notes:[{id:'',text:'x',color:'#ffe783',status:'todo',history:[]}]},{...EMPTY,folders:[{id:'f',name:'n',pages:[]}]},{...EMPTY,expenses:[{id:'e',concept:'',cents:-1}]}])('rechaza estructuras internas inválidas',bad=>expect(()=>parseBackup(JSON.stringify(bad))).toThrow());it('rechaza backup incompatible',()=>expect(()=>parseBackup('{}')).toThrow())});
